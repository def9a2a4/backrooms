settings:
  example_option: true

backrooms:
  # Entry triggers (overworld -> backrooms)
  entry:
    enabled_worlds:
      - world
      - world_nether
    suffocation_noclip:
      enabled: true
      chance: 0.02
      target_level: level_64637
    void_fall:
      enabled: true
      target_level: level_64637
    bed_anomaly:
      enabled: true
      chance: 0.05
      require_thunderstorm: true
      target_level: level_64637
    portal_mislink:
      enabled: true
      chance: 0.01
      target_level: level_64637

  # Entity spawner settings
  entity_spawner:
    max_entities_per_player: 3
    spawn_check_interval_ticks: 100
    spawn_distance_min: 30
    spawn_distance_max: 60

  # Player settings
  player:
    death_respawn_level: level_0
    escalation_thresholds_minutes:
      - 5
      - 15
      - 30
      - 60
      - 120
